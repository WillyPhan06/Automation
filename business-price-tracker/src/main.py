from utils import log_info

def main():
    log_info("Starting Business Price Tracker automation...")
    # Later we’ll: fetch → clean → report → email
    log_info("Setup complete. Ready for next steps!")

if __name__ == "__main__":
    main()
